import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Agent Blueprint Setup — Stop Using AI Manually",
  description:
    "Done-with-you 90-minute implementation session. Get a personalized multi-agent org chart, prompt packs, handoff templates, and cron schedules.",
  openGraph: {
    title: "Agent Blueprint Setup — Stop Using AI Manually",
    description:
      "Done-with-you 90-minute implementation session to automate your business with AI agents.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
